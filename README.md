# Headless API

WordPress REST API plugin for the `tlu/v1` and `headless/v1` namespaces.

Install the release ZIP through WordPress, replacing any existing `headless-api` plugin directory. Do not install a second copy under another folder name.
